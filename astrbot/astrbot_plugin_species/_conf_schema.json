{
  "taxonomy_home": {
    "description": "taxonomy/ 包所在目录",
    "type": "string",
    "default": "",
    "hint": "例如 ~/apps/species，留空则使用 TAXONOMY_HOME 环境变量"
  },
  "db_path": {
    "description": "taxonomy.db 数据库路径",
    "type": "string",
    "default": "",
    "hint": "留空则使用 TAXONOMY_DB 环境变量或 taxonomy 包默认路径"
  }
}
